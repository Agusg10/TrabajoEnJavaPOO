package DomainClasses;

import java.util.List;

public interface Filtrable {
   void CharacterRange(Publication PublicationSize);
   void ChangeFont(Publication Pub);
   void Fontsize(Publication PublicationImage);
}
